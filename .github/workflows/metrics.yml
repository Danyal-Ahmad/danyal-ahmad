name: Metrics

# Regenerates github-metrics.svg daily with live data from the GitHub API.
# The repo ships with a static, hand-crafted SVG already containing real
# data — this workflow overwrites it with an auto-generated dashboard
# from lowlighter/metrics so the numbers stay fresh over time.
#
# Requires the METRICS_TOKEN secret (see SETUP.md).

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  github-metrics:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Generate metrics dashboard
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          user: Danyal-Ahmad
          template: classic
          base: header, activity, repositories, metadata
          config_timezone: Asia/Karachi
          plugin_languages: yes
          plugin_languages_details: bytes-size, percentage
          plugin_languages_limit: 8
          filename: github-metrics.svg

      - name: Commit updated metrics SVG
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add github-metrics.svg
          git commit -m "chore: refresh github-metrics.svg" || echo "no changes"
          git push
